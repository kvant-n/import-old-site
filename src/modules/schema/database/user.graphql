
type User {
  id: ID! @unique
  Imports: [Import!]! @relation(name: "UserImports")
  oldID: Int @unique
  oldEmail: String
  # username: String @unique
  # email: String @unique
  # fullname: String
}